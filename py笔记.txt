
map(function,sequence)  map执行完一次后就没了
dt['k']=dt.get('k',0)+1
字典的键必须是不可变的数据类型，值随意
集合的元素必须是不可变的数据类型
集合元素没于顺序，所以不能下标访问

自定义类不能比较大小，因为其__lt__，__gt__等函数被自动设置成了None
默认情况下，自定义类的__eq__是判断两个对象id是否相同，即 is的作用
自定义的类，默认情况下，哈希值是根据对象的id算的，而不是对象的值
数组不能增删，实际上是形成新数组
numpy数组的切片是视图，即原数组的一部分
pandas中series对象的切片也是视图，二维表格中的某一列某一行也都是视图
pandas中 .loc是基于标签  .iloc是基于位置